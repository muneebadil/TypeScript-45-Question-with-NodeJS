let array = ["Peproni", "Tikka", "Fajita"];

array.forEach(each => {
    if (each === "Peproni") {
        console.log(`I love ${each} Pizza`);
    }
    else if (each === "Tikka") {
        console.log(`I love ${each} Pizza`);
    }
    else {
        console.log(`I love ${each} Pizza`);
    }
});

console.log("Pizza is Craze");